<%= form_with(model:[book, book_comment], local:false) do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメント", class: "w-100" %>
  <%= f.submit "送信する", class: "btn btn-sm btn-success mt-20 pull-right" %>
<% end %>